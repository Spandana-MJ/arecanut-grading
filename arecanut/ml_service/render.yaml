services:
  - type: web
    name: arecanut-ml-service
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python predict.py
    envVars:
      - key: PYTHON_ENV
        value: production
